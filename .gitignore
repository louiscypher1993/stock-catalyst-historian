node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Firebase config (contains live API credentials)
firebase-applet-config.json

# SQLite database and journal files
*.db
*.db-shm
*.db-wal

# Local cache files
market_ecosystem_cache.json

# Python bytecode cache
__pycache__/

# Downloaded SEC DERA bulk data sets (large, re-downloadable)
sec_13f_data/

# Scratch ML experiment models -- never deployed, not part of the real pipeline
src/ml/scratch/